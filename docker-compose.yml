services:
  scira:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - '3000'
    environment:
      NODE_ENV: production
      PORT: '3000'
      HOSTNAME: 0.0.0.0
    restart: unless-stopped
